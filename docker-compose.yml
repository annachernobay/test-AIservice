version: '3.8'

services:
  web:
    build: .
    container_name: ai_chat_service
    ports:
      - "8000:8000"
    environment:
      - OPENROUTER_API_KEY=${OPENROUTER_API_KEY}
    volumes:
      - .:/app
    restart: unless-stopped